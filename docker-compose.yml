services:
  vbc_liveqa:
    build: .
    image: liveqa:local
    ports:
      - "3000:3000"
    volumes:
      - liveqa_data:/app/database
    environment:
      - MODERATOR_PASSWORD=mod123

volumes:
  liveqa_data:
